{
  "compilerOptions": {
    "target": "ES2022",
    "outDir": "./dist", // Specify the output directory for compiled files
    //"strict": true, // Enable strict type checking
    "esModuleInterop": true, // Enable interoperability between CommonJS and ES modules
    "rootDir": "./src", // Specify the root directory of the source files
    "module": "Node16" // Specify the module system
  }
  // "include": [
  //     "src/**/*.ts" // Specify the files to be included in the compilation process
  // ],
  // "exclude": [
  //     "node_modules" // Specify the files to be excluded from the compilation process
  // ]
}
